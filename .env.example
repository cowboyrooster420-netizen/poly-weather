# Anthropic API key (for Claude Haiku market classification/parsing fallback)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override default Kelly fraction (default 0.25 = quarter-Kelly)
# KELLY_FRACTION=0.25

# Optional: minimum edge threshold to generate a signal (default 0.05 = 5%)
# MIN_EDGE=0.05

# Optional: SQLite database path (default: ~/.weather-edge/signals.db)
# DB_PATH=~/.weather-edge/signals.db

# Optional: NOAA/NWS user agent (they ask for contact info)
# NWS_USER_AGENT=weather-edge (your@email.com)

# Telegram notifications (optional)
# Get a bot token from @BotFather on Telegram
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# Chat ID: send a message to your bot, then visit
# https://api.telegram.org/bot<token>/getUpdates to find your chat_id
# TELEGRAM_CHAT_ID=123456789
# TELEGRAM_ENABLED=true
# Minimum edge for individual high-edge alerts (default 0.10 = 10%)
# TELEGRAM_HIGH_EDGE=0.10
